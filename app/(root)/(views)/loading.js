import Loading from "@/app/(dashboard)/dashboard/loading";

export default function AppRouterLoading() {
  // Note: The "use client" directive is not needed here
  // because this is a special Next.js file
  return <Loading />;
}
